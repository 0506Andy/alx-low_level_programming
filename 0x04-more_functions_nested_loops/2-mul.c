#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: First integer is a multiplicand
 * @b: Second integer is a multiplier
 * Return: Product
 */
int mul(int a, int b)
{
	int product;

	product = (a * b);

	return (product);
}
